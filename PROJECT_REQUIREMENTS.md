# 项目级开发要求

本文档是本项目的强制开发规范。所有新功能、功能调整和缺陷修复都必须遵守以下要求；如果现有实现与本文档冲突，后续修改应逐步向本文档要求靠拢。

## 1. Git 分支要求

### 1.1 功能必须使用独立分支

- 每个独立功能必须创建一个新的功能分支。
- 功能分支名必须以 `feat/` 开头。
- 分支名应简洁、明确，使用小写字母、数字和连字符描述功能，例如：
  - `feat/csv-import`
  - `feat/ticket-ocr`
  - `feat/journey-attachments`
- 不允许直接在 `master` 分支开发功能或提交功能代码。
- 开始开发前，应确认当前分支不是 `master`，且功能分支基于最新的 `master` 创建。

### 1.2 提交和合并流程

每个功能必须按以下顺序完成：

1. 从最新的 `master` 创建对应的 `feat/*` 分支。
2. 在功能分支上完成实现。
3. 运行与改动相关的测试、类型检查、构建和必要的 lint 检查。
4. 确认所有检查成功，并检查最终 diff。
5. 提交功能代码。
6. 创建 Pull Request，将功能分支合并到 `master`。
7. 只有测试成功并完成检查后，才允许合并到 `master`。

测试或构建失败时：

- 不得提交“看起来完成”的代码。
- 不得绕过、删除或弱化失败的测试来获得通过。
- 应先修复问题，再重新运行完整的相关检查。
- 如果失败属于环境或已有问题，必须在提交或 Pull Request 中明确记录原因、影响和已执行的验证。

## 2. 代码组织与组件化

- 功能必须尽可能组件化，避免将页面、业务逻辑、数据访问和复杂 UI 全部堆在一个文件中。
- Vue 页面负责页面编排；可复用的展示、表单和交互单元应拆分为独立组件。
- 复杂或可复用的业务逻辑应抽取为 composable、工具函数或独立服务模块。
- Supabase 查询、认证、Storage 和 Edge Function 调用应集中在合适的数据访问模块或 composable 中，避免在多个组件中重复实现。
- 组件应有清晰的输入和输出，优先使用明确的 props、emits 和 TypeScript 类型。
- 不得为了快速实现而复制粘贴两套相同逻辑；航班和火车等相似流程应在保持领域差异的前提下复用通用组件或工具。
- 单个组件过于复杂时，应优先拆分职责，而不是继续增加条件分支和状态。
- 新增代码必须保持现有 TypeScript 类型安全，不使用无必要的 `any`、`as any` 或 `as unknown` 绕过类型检查。

## 3. UI 技术栈与设计要求

- UI 必须使用项目现有的 Vue 组件体系、shadcn-vue 组件和 Tailwind CSS。
- 优先复用 `src/components/ui/` 中已有的 shadcn-vue 组件；只有在没有合适组件时才新增组件。
- 新增 UI 组件应遵循 shadcn-vue 的结构和项目现有样式模式。
- 布局、间距、颜色、响应式行为和状态样式优先使用 Tailwind CSS utility classes。
- 不得为简单 UI 样式引入新的 CSS 框架或组件库。
- 不得为了单个功能引入与现有技术栈重复的 UI 依赖。
- 所有新增交互都应考虑：
  - 桌面端和移动端响应式布局；
  - 键盘操作和可见焦点；
  - 表单标签、错误提示和基本可访问性；
  - 加载中、成功、失败和空状态；
  - 浅色和深色主题的一致表现。
- UI 文案、错误提示和交互反馈应与现有应用风格保持一致，不得使用静默失败。

## 4. 功能实现要求

- 开始编码前，必须先阅读相关页面、组件、路由、数据访问代码和现有样式，复用已有模式。
- 功能必须覆盖完整用户流程，而不是只实现一个孤立的界面。
- 所有用户输入都必须进行适当校验；无效输入应给出明确、可操作的提示。
- 异步操作必须处理加载状态、成功状态和失败状态。
- 错误必须显式反馈，不得使用宽泛的 `catch` 静默吞掉异常，也不得用成功结果伪装失败。
- 涉及 Supabase 的功能必须考虑用户身份、数据隔离、RLS、Storage 权限和敏感信息保护。
- 不得将 API 密钥、服务端密钥或其他凭据放入前端代码、提交记录或公开配置。
- 修改数据库结构、Storage 或 Edge Function 时，必须同步更新相关文档和部署/配置说明。

## 5. 测试与验证要求

每次完成一个功能后，必须执行与改动范围匹配的验证，至少包括：

- 相关功能测试或手动验收；
- TypeScript 类型检查；
- 项目构建；
- 改动涉及的 lint、格式检查或其他仓库已有检查。

验证要求：

- 优先运行覆盖本次改动的最小测试集；如果改动影响公共组件、路由、数据层或全局样式，应扩大验证范围。
- 测试必须覆盖正常流程、输入错误、加载状态、失败状态和关键边界情况。
- 前端功能至少验证桌面端和移动端布局；涉及主题的功能同时验证浅色和深色模式。
- 测试结果必须记录在开发过程或 Pull Request 描述中，包括执行的命令和结果。
- 任何测试失败都必须修复或明确阻塞原因，不能以“与本次功能无关”为由直接合并。

当前项目的基础验证命令：

```bash
npm run build
```

如果后续新增测试或 lint 命令，应将其纳入功能分支的验证流程，并在 README 或本文件中补充说明。

## 6. 提交与 Pull Request 要求

- 提交应保持小而完整，每个提交围绕一个明确目的。
- 提交前必须检查 `git diff` 和 `git status`，确认没有临时文件、构建产物、凭据或无关改动。
- Pull Request 必须说明：
  - 功能目的和实现范围；
  - 主要改动文件或模块；
  - 数据库、环境变量或部署变更；
  - 已执行的测试/构建命令及结果；
  - 已知限制和后续工作。
- UI 改动应附上关键页面的截图或录屏，并说明验证的响应式和主题场景。
- Pull Request 只能从 `feat/*` 分支合并到 `master`，不得直接在 `master` 上提交或绕过验证合并。

## 7. 交付前检查清单

- [ ] 当前工作位于正确的 `feat/*` 分支。
- [ ] 功能已按职责拆分为合适的页面、组件、composable 或服务模块。
- [ ] UI 使用 shadcn-vue 和 Tailwind CSS，并复用了现有组件。
- [ ] 正常、错误、加载、空状态和关键边界情况已处理。
- [ ] 类型检查通过。
- [ ] 相关测试通过。
- [ ] `npm run build` 通过。
- [ ] 已验证桌面端、移动端以及适用的浅色/深色主题。
- [ ] 已检查 `git diff`，没有敏感信息和无关文件。
- [ ] Pull Request 已说明测试结果和已知限制。
- [ ] 只有在以上检查完成后，才允许合并到 `master`。
